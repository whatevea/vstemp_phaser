const vars ={
     paddleOffset:75, //distance from border to paddle
     xDifficulty:4,
     yDifficulty:6,
     ballRadius:30,
     centerHitPercent:70, //this percentage determines if it is hit in center of paddle or side
    initialBallVelocity:230
    }
export default vars;